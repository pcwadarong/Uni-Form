import { CreatePageContainer } from "@/features/survey/create/components/CreatePageContainer";

/**
 * 설문 생성 페이지
 * 설문 생성 기능을 제공하는 페이지 컴포넌트
 */
export default function Create() {
  return <CreatePageContainer />;
}
